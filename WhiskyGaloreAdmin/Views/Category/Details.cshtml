@model WhiskyGaloreAdmin.Models.Category
@{
    ViewBag.Title = "Tables";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<script src="@Url.Content("~/Scripts/readonly.js")" type="text/javascript"></script>
<div class="row">
    <div class="col-lg-12">
        <h1 class="page-header">Categories</h1>
    </div>
    <!-- /.col-lg-12 -->
</div>
<!-- /.row -->
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                Click 'Edit' to change the details of a product
            </div>
            <!-- /.panel-heading -->
            <div class="panel-body">
                <div class="dataTable_wrapper">
                    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                        <thead>
                            <tr>
                                <th>Category Id</th>
                                <th>Category Name</th>
                                <th>Category in use</th>
                                <th>Description</th>
                               
                                <th></th>
                            </tr>
                        </thead>
                        <tbody>
                            @for (int i = 0; i < Model.dt.Rows.Count; i++)
                            {
                                <tr class="odd gradeX">
                                    <td>@(Model.dt.Rows[i]["categoryId"])</td>
                                    <td>@(Model.dt.Rows[i]["categoryName"])</td>
                                    <td>@(Model.dt.Rows[i]["categoryInUse"])</td>
                                    <td>@(Model.dt.Rows[i]["catDescription"])</td>
                                    <td>@Html.ActionLink("Edit", "Edit", null, new { categoryId = Model.dt.Rows[i]["categoryId"] }, null)</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
                <!-- /.table-responsive -->
            </div>
            <!-- /.panel-body -->
        </div>
        <!-- /.panel -->
    </div>
    <!-- /.col-lg-12 -->
</div>

